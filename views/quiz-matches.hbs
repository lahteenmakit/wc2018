{{> quiz-header}}

<div id="matchesTableDiv" class="container">
  <div class="progress">
    <div class="progress-bar" role="progressbar" style="width: 0%" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
  </div>
  <h1>Group stage match results</h1>
  <p>Guess the result of each match played in the group stage. Enter the number of goals scored by both teams.</p>
  <p>Points: <br>
    Correct number of goals for each team: <b>4 points</b><br>
    Correct number of goals for only one team: <b>1 point</b><br>
    Correct outcome (1-X-2): <b>1 point</b><br>
  </p>
  <div id="matchesForm" class="container">
    <form method="POST" action="/quiz/matches">
      <table class="table table-striped">
        <tbody>
          {{#each matches}}
            <tr>
              <td>{{this.date}} <br>
                  {{this.groupNumber}} <br>
                  {{this.location}} <br>
              </td>
              <td>
                  <img src="/public/images/flags/{{parseImgFileName this.homeTeam}}" height="20" width="40">
              </td>
              <td>{{this.homeTeam}}
                  <input class="form-control" name="{{addOne @index}}-homeGoals" type='number' min='0' placeholder="Goals" required>
              </td>
              <td>{{this.awayTeam}}
                  <input class="form-control" name="{{addOne @index}}-awayGoals" type='number' min='0' placeholder="Goals" required>
              </td>
              <td>
                  <img src="/public/images/flags/{{parseImgFileName this.awayTeam}}" height="20" width="40">
              </td>
            </tr>
          {{/each}}
        </tbody>
      </table>
      <button type="submit" class="btn btn-primary">Submit your answers</button>
    </form>

  </div>
</div>
